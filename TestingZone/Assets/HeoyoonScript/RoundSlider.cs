using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoundSlider : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
public class SelectUI
{
    public UnityEngine.UI.Image[] images = new UnityEngine.UI.Image[8];
    public UnityEngine.UI.Image pickedImages;
    public byte pickedUI;
}
